# FastAPI attendance backend + on-device face-recognition stack.
#
# Everything here resolves to prebuilt binary wheels — no compiler or cmake is
# required, including on CPython 3.13/3.14. Two notes:
#   * `dlib-bin` ships a prebuilt dlib (upstream `dlib` has no 3.14 wheel and
#     would need a from-source build). `face-recognition` is then installed with
#     --no-deps by setup.sh so its hard `dlib` dependency doesn't drag in that
#     source build.
#   * opencv 5.x is the first line with numpy-2 / cpython-3.14 compatible wheels.
#
# Run ./setup.sh (which installs this file plus face-recognition --no-deps).
fastapi>=0.115,<1.0
setuptools<81
uvicorn[standard]>=0.30,<1.0
sqlalchemy>=2.0,<3.0
psycopg[binary]>=3.2,<4.0
pydantic-settings>=2.4,<3.0
email-validator>=2.2,<3.0
python-multipart>=0.0.9,<1.0
passlib[bcrypt]>=1.7,<2.0
PyJWT>=2.9,<3.0
httpx>=0.27,<1.0
numpy>=1.26,<3.0
Pillow>=10.0
click>=8.0
opencv-python==5.0.0.93
dlib-bin>=20.0,<21
face-recognition-models>=0.3.0
# face-recognition itself is installed by setup.sh with --no-deps.
# Import name: face_recognition
